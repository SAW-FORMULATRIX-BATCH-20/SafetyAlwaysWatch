# Do not persist violation snapshots

SAW V2.1 will not store or display captured violation snapshots, despite older FR-26 and conceptual UI text that describe snapshot evidence. This prioritizes employee privacy and simpler retention obligations; the audit experience will rely on event metadata, episode status, score changes, and ledger history instead.
